version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Static mirror of tickerplay.com — no build step"
  artifacts:
    baseDirectory: website
    files:
      - "**/*"
  cache:
    paths: []
